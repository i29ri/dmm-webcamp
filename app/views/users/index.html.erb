<p id="notice">
 <%=  notice %>
</p>

  <%= render 'layouts/form', user: @current_user ,book: @book_new %>
 <div class="col-xs-9">
 <p id="notice">

  <div class="top-title">
  <h1>Users</h1>
  </div>

  <table class="table">
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
    </tr>

    <tbody>
      <% @users.each do |user| %>
         <tr>
          <td><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class: "img-circle pull-left profile-thumb" %></td>
          <td><%= user.name %></td>
          <td><%= link_to "Show", user_path(user.id) %></td>
        </tr>
      <% end %>
    </tbody>

  </table>


</div>
 </div>
</div>